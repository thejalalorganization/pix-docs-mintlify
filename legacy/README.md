# Legacy draft (not published)

Auto-generated by Mintlify's agent from the public marketing page when the repo
was connected. Kept here for comparison only; `.mintignore` excludes it.

Treat its facts as unverified. Known problems: the keyboard shortcuts table is
not sourced from the app, the Mac install page describes an "unidentified
developer" prompt that a notarized build does not show, and
`sources/site/pixcode.sh/` is a scrape of the marketing site.

Useful patterns worth borrowing into the generator: `<Tabs>` per platform on
install, `<Steps>` for procedures, per-agent pages.
